# CLI Skills 入口

本仓库已提供可供 AI 自动调用的 CLI 发布 Skill：

- `.cursor/skills/matrixmedia-cli-publish/SKILL.md`

配套示例：

- `.cursor/skills/matrixmedia-cli-publish/examples.md`

如果你的 AI 平台支持读取仓库内 Skill 目录，可直接加载上述文件并按其中流程执行 `cli login` / `cli publish`。
